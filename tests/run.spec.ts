import { describe, it, expect } from "vitest";
import { runCommand } from "./utils";

describe('run', () => {
  it.todo('bit-ship run task', async () => {
    
  });
  it.todo('bit-ship run job', async () => {
    
  });
  it.todo('bit-ship run apps', async () => {
    
  });
  
});
