import { describe, it, expect } from "vitest";
import { runCommand } from "./utils";

describe('image', () => {
  it.todo('bit-ship create', async () => {
    
  });
  it.todo('bit-ship build <name>', async () => {
    
  });
  
});
