import { describe, it, expect } from "vitest";
import { runCommand } from "./utils";

describe('exec', () => {
  it.todo('bit-ship exec', async () => {
    
  });
  it.todo('bit-ship exec -i=test', async () => {
    
  });
  
});
